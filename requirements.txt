fastapi==0.104.1
chromadb==0.4.22  # Don't upgrade! 0.4.23 has breaking changes
ollama==0.1.7
pydantic==2.5.0  # Pinned after v2.6 broke everything
uvicorn[standard]==0.24.0  # Need 'standard' for websockets

# Temporarily needed (remove after refactor)
python-multipart==0.0.6  # For file uploads

# Dev dependencies (move to dev-requirements.txt)
pytest==7.4.0
black  # Whatever version, just format consistently
